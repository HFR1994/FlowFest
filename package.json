{
  "name": "festival-companion-expo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd mobile && expo start --tunnel",
    "android": "cd mobile && expo start --android",
    "ios": "cd mobile && expo start --ios",
    "web": "cd mobile && expo start --web",
    "install-mobile": "cd mobile && npm install"
  },
  "workspaces": [
    "mobile"
  ],
  "devDependencies": {
    "typescript": "~5.3.3"
  }
}
